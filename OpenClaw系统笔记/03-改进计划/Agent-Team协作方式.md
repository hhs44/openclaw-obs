# Agent Team 内部协作方式详解

---

## 🔄 协作架构

```
                        ┌─────────────────────────────────────┐
                        │            用户                      │
                        │  (可随时介入任何Agent)               │
                        └──────────┬──────────────────────────┘
                                   │
                                   │ sessions_send
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│                          Team Leader                                  │
│                                                                      │
│  职责：分发任务、跟踪进展、协调依赖、解决阻塞                           │
└──────────┬───────────────────────────────┬───────────────────────────┘
           │                               │
           │ sessions_send                 │ sessions_send
           │ (任务指令)                     │ (任务指令)
           ▼                               ▼
┌─────────────────────┐         ┌─────────────────────┐
│     Engineer        │ ◄─────► │      Creator        │
│                     │  Agent间 │                     │
│   - 游戏设计        │  直接通信 │   - UI开发          │
│   - 核心逻辑        │         │   - 动画特效        │
└─────────────────────┘         └─────────────────────┘
           │                               │
           │ 任务完成后                    │ 任务完成后
           │ sessions_send                 │ sessions_send
           ▼                               ▼
     Team Leader                    Team Leader
```

---

## 📋 协作场景详解

### 场景1: Team Leader 分发任务

**Team Leader → Engineer:**
```javascript
sessions_send({
  sessionKey: "agent:engineer:main",
  message: `
【三国2048项目】任务分配

你是游戏设计师，负责：
1. 设计16级人物体系
2. 定义合成规则（0.1%跳级）
3. 设计收集系统

项目目录：/home/huang/.openclaw/projects/sanguo-2048/
输出文件：docs/game-design.json

完成后回复"任务完成"并汇报结果。
  `,
  timeoutSeconds: 300  // 等待5分钟确认
})
```

**Engineer 回复:**
```
任务已收到，开始执行游戏设计。
预计30分钟完成。
```

---

### 场景2: Engineer 完成后通知 Team Leader

**Engineer → Team Leader:**
```javascript
sessions_send({
  sessionKey: "agent:teamleader:main",
  message: `
【任务完成汇报】

任务：游戏设计
状态：✅ 完成
输出：docs/game-design.json
耗时：28分钟

发现的问题：无
下一步建议：可以开始核心逻辑开发
  `
})
```

---

### 场景3: Team Leader 通知依赖任务开始

**Team Leader → Creator:**
```javascript
sessions_send({
  sessionKey: "agent:creator:main",
  message: `
【三国2048项目】可以开始工作了

依赖任务已完成：
- ✅ 游戏设计（Engineer完成）
- ✅ 核心逻辑（Engineer完成）

你的任务：
1. 实现游戏UI
2. 实现动画特效
3. 实现收集系统界面

参考文件：
- /projects/sanguo-2048/docs/game-design.json
- /projects/sanguo-2048/src/core/core.js

现在可以开始。
  `
})
```

---

### 场景4: Agent 间直接协作

**Engineer → Creator（直接沟通）:**
```javascript
sessions_send({
  sessionKey: "agent:creator:main",
  message: `
【核心逻辑说明】

我已完成核心逻辑，这里说明关键接口：

1. 游戏初始化：
   const game = new SanGuo2048();

2. 移动：
   game.move('up');

3. 收集系统：
   const collection = new CollectionSystem();
   collection.unlockRandomCharacters(2);

4. 事件回调：
   game.onMerge((level, isJump) => {
     // 触发动画
   });

有问题随时问我。
  `
})
```

---

### 场景5: 用户介入协助

**用户 → Engineer:**
```javascript
sessions_send({
  sessionKey: "agent:engineer:main",
  message: `
【用户指导】

游戏设计这里需要调整：
- 人物等级改成真实三国人物
- 特效要更华丽

请修改 game-design.json
  `
})
```

---

### 场景6: Agent 遇到阻塞请求帮助

**Creator → Team Leader:**
```javascript
sessions_send({
  sessionKey: "agent:teamleader:main",
  message: `
【阻塞请求】

问题：动画特效实现需要设计稿
阻塞原因：Engineer的设计文档没有特效具体参数

需要：
1. Engineer补充特效参数
2. 或用户提供特效参考

当前进度：50%
  `
})
```

**Team Leader 处理:**
```javascript
// Team Leader → Engineer
sessions_send({
  sessionKey: "agent:engineer:main",
  message: "请补充特效参数到设计文档，Creator在等待"
})

// Team Leader → Creator
sessions_send({
  sessionKey: "agent:creator:main",
  message: "已通知Engineer补充，请等待"
})
```

---

## 📊 协作规则总结

| 通信方向 | 使用工具 | 场景 |
|---------|---------|------|
| Team Leader → Agent | `sessions_send` | 分发任务、通知依赖、解决阻塞 |
| Agent → Team Leader | `sessions_send` | 汇报完成、请求帮助 |
| Agent → Agent | `sessions_send` | 直接协作、技术交流 |
| 用户 → Agent | `sessions_send` | 介入协助、调整方向 |
| Team Leader → C-3PO | `sessions_send` | 定时汇报进展 |

---

## 🔧 技术实现

### sessions_send 参数说明

```javascript
sessions_send({
  sessionKey: "agent:<agentId>:main",  // 目标Agent
  message: "消息内容",                  // 任务/问题/通知
  timeoutSeconds: 300                   // 0=异步，>0=等待回复
})
```

### 常用 sessionKey

| Agent | sessionKey |
|-------|------------|
| C-3PO | `agent:dev:main` |
| Team Leader | `agent:teamleader:main` |
| Engineer | `agent:engineer:main` |
| Creator | `agent:creator:main` |

---

## 📝 协作时机

```
任务依赖触发：
  设计完成 ──→ Team Leader通知 ──→ 开发开始

阻塞解决：
  Agent请求 ──→ Team Leader协调 ──→ 问题解决

直接协作：
  Engineer ──→ Creator（技术说明）
  Creator ──→ Engineer（接口确认）

用户介入：
  用户 ──→ 任意Agent（随时）
```

---

## ✅ 协作检查清单

- [ ] Team Leader 分发任务后收到确认
- [ ] Agent 完成后主动汇报
- [ ] 依赖任务完成时通知下游Agent
- [ ] Agent遇到阻塞时请求Team Leader
- [ ] 定时向C-3PO汇报进展
- [ ] Obsidian实时更新任务状态

---

**这个协作方式是否清晰？**

请确认后开始实施。
